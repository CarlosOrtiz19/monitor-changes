package com.pagewatcher.repository;

import com.pagewatcher.model.ImageCrop;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageCropRepository extends JpaRepository<ImageCrop,Long> {
}
