package com.pagewatcher.repository;

import com.pagewatcher.model.CropQuartz;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CropQuartzRepository extends JpaRepository<CropQuartz,Long> {
}
